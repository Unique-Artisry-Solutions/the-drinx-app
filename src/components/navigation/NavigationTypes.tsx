
// This file now only contains the NavigationType enum and exports it
import React from 'react';

// Types of navigation
export enum NavigationType {
  GUEST = 'guest',
  USER = 'user',
  ADMIN = 'admin',
}

export default NavigationType;
