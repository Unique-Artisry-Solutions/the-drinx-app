
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const ProfileTabs: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Profile Tabs</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the profile tabs component.</p>
      </CardContent>
    </Card>
  );
};

export default ProfileTabs;
